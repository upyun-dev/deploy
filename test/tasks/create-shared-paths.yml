---
- name: UPYUN-DEV-DEPLOY | Ensure shared path 1 exists
  file:
    state: directory
    path: "{{ ansistrano_deploy_to }}/shared/blah"

- name: UPYUN-DEV-DEPLOY | Ensure shared path 2 exists
  file:
    state: directory
    path: "{{ ansistrano_deploy_to }}/shared/foo/bar"

- name: UPYUN-DEV-DEPLOY | Ensure shared path 3 exists
  file:
    state: directory
    path: "{{ ansistrano_deploy_to }}/shared/xxx/yyy/zzz"

- name: UPYUN-DEV-DEPLOY | Ensure shared file 1 exists
  file:
    state: touch
    path: "{{ ansistrano_deploy_to }}/shared/test.txt"

- name: UPYUN-DEV-DEPLOY | Ensure shared files folder exists
  file:
    state: directory
    path: "{{ ansistrano_deploy_to }}/shared/files"

- name: UPYUN-DEV-DEPLOY | Ensure shared file 2 exists
  file:
    state: touch
    path: "{{ ansistrano_deploy_to }}/shared/files/test.txt"
